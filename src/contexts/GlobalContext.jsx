// Prendo createContext da react
import { createContext } from "react";

// Variabile che sostituisce un oggetto
const CountContext = createContext();

// Esporto la variabile
export default CountContext;